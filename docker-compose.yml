services:
  it-guide-tool:
    build: .
    container_name: it-guide-tool
    user: "1000:1000"
    ports:
      - "5000:5000"
    volumes:
      - ./public/data:/app/public/data
      - ./backups:/app/backups
    restart: unless-stopped
